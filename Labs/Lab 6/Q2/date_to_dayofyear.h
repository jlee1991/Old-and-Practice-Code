/**
 * Converts a date to the day of the month.
 *
 * Note that you do not have to perform any error checking
 * if the input month or day are out of range.
 *
 * @param month The input month, where 1-12 corresponds to Jan-Dec.
 * @param day The input day of the month, from 1-31 (depending on the month).
 * @returns The day of the year (will be an output value 1-365).
 */

// YOUR TASK: COMMENT OUT ALL BUT ONE OF THESE PROTOTYPES!

//int * date_to_dayofyear(int *month, int *day);

//void date_to_dayofyear(int month, int day, int year);

int date_to_dayofyear(int month, int day);

//int date_to_dayofyear(int &month, int *day);
